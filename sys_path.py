#!./venv/bin/python

# If current directory is the first directory in the module search
# path and it's causing problems, we can remove it as follows.
# import sys
# sys.path.pop(0)

# But better to just not have python programs with names the same
# as built-in modules in the current directory.

# So, no "types.py" for instance.

import re
